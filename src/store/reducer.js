const defaultState = {
    currentUser: null,
};

const reducer = (state = defaultState, action) => {
    switch (action.type) {
        // case value:
        //     break;
        default:
            return state;
    }
}

export default reducer;